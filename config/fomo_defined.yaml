stage: pretrain

paths:
  data_dir: "./data"
  ckpt_dir: "./checkpoint"
  log_dir: "./logs"
  mapping_file: "./fomo_defined.json"
  resume_checkpoint_path: null

data:
  img_size: [128, 128, 128]
  patch_size: [16, 16, 16]
  batch_size: 4
  num_workers: 8
  pin_memory: true
  modality_encoder_model: "dmis-lab/biobert-v1.1"

model:
  name: "mae_vit3d_base"
  in_channels: 1
  out_channels: 1
  spatial_dims: 3
  mask_ratio: 0.78 
  embed_dim: 768

optimizer:
  type: "adamw"
  lr: 1.0e-4
  weight_decay: 0.05
  betas: [0.9, 0.999]
  eps: 1.0e-8

scheduler:
  type: "warmupcosine"
  max_epochs: 100
  warmup_epochs: 10
  min_lr: 1.0e-6
  start_factor: 0.001
  end_factor: 1.0

optimizer:
  type: "adamw"
  lr: 1.0e-4       # increased for 8.4K scale
  weight_decay: 0.05
scheduler:
  type: "warmupcosine"
  max_epochs: 100
  warmup_epochs: 10
  min_lr: 1.0e-6


train:
  epochs: 10
  seed: 42
  device: "cuda" # Change to "cuda" if GPU is available
  log_freq: 100
  save_freq: 10
  clip_grad_norm: 1.0